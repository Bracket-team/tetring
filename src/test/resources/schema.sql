CREATE TABLE `relics` (
                          `relic_number`	INT	NOT NULL,
                          `relic_name`	VARCHAR(20)	NOT NULL ,
                          `rarity`	VARCHAR	NOT NULL ,
                          `price`	TINYINT	NOT NULL ,
                          `effect`	TEXT	NULL
);